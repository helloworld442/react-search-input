import { styled } from "styled-components";

//button도 많이 쓰일 것이라고 예상해서 따로 빼둠
const StyledButtonBox = styled.button`
  padding: 8px 0;
  background: transparent;
  border: none;
  outline: none;
  border-radius: 5px;
  font-size: 1.6rem;
`;

export default StyledButtonBox;
